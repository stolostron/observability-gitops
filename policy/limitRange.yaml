apiVersion: v1
kind: LimitRange
metadata:
  name: limit-range
  namespace: open-cluster-management-observability
spec:
  limits:
  - max:
      cpu: "2"
      memory: "7Gi"
    defaultRequest:
      cpu: "10m"
      memory: "50Mi"
    type: Container
